# Runtime dependencies for FinanzIAs.
# Use pinned versions in requirements.lock for reproducible installs.
# Run scripts/audit_dependencies.py periodically to check for CVEs.

# UI
PyQt6>=6.6.0,<7.0

# Market data
yfinance>=0.2.40           # frequent releases — Yahoo Finance API drifts
requests>=2.32.0           # was 2.31.0; bumped for CVE-2024-35195 (cert verify)

# Database
SQLAlchemy>=2.0.0,<3.0
# T7.3: alembic pasó de dev-only a runtime — init_db corre `upgrade head` en
# cada arranque (único camino de esquema desde la migración catch-up 0004).
alembic>=1.13

# Data wrangling
# IMPORTANT — Python 3.12 / 3.13 compatibility:
#   * pandas 2.2.2 is the first release with numpy-2.0 support AND wheels
#     across 3.9-3.13. Pin >=2.2.2 so pip never builds from source on Win
#     and so numpy 2.x is usable.
#   * numpy: the old <2 pin is gone. It was kept "because pandas 2.2.x
#     constrains numpy<2", which stopped being true at pandas 2.2.2. numpy
#     <2 also has no Python 3.13 wheels, so it cannot install on 3.13 at all.
#     The codebase uses no numpy-2.0-removed APIs (audited: np.float_,
#     np.NaN, np.in1d, np.trapz, etc. -> zero usages) and the runtime env
#     already runs numpy 2.2.x. <3.0 guards against a future 3.0 break.
#   * pandas-ta is INTENTIONALLY NOT in this list. The codebase computes
#     RSI/MACD/Bollinger/SMA/EMA with pure pandas (``close.ewm(...)``,
#     ``close.rolling(...)``) in ``analysis/technical.py``. Adding
#     pandas-ta would re-introduce a numpy 2.x conflict its betas trigger.
pandas>=2.2.2,<3.0
numpy>=1.26.4,<3.0

# Charts
matplotlib>=3.8.0,<4.0
mplfinance>=0.12.10b0

# Reports
reportlab>=4.0.0
openpyxl>=3.1.0

# Quant / ML
hmmlearn>=0.3.0
arch>=6.0

# Optional — LLM catalyst classifier backend (Sprint 5 · T-CAT-2).
# Only needed to run `classify_catalysts.py --backend llm|hybrid`; the default
# heuristic backend needs nothing. Install on demand: `pip install anthropic`
# and set ANTHROPIC_API_KEY. Left commented so it isn't force-installed.
# anthropic>=0.40
