# Runtime dependencies for FinanzIAs.
# Use pinned versions in requirements.lock for reproducible installs.
# Run scripts/audit_dependencies.py periodically to check for CVEs.

# UI
PyQt6>=6.6.0,<7.0

# Market data
yfinance>=0.2.40           # frequent releases — Yahoo Finance API drifts
requests>=2.32.0           # was 2.31.0; bumped for CVE-2024-35195 (cert verify)

# Database
SQLAlchemy>=2.0.0,<3.0

# Data wrangling
# IMPORTANT — Python 3.12+ compatibility:
#   * pandas 2.1.0 only has wheels for 3.9-3.12 (the 3.12 wheel landed in
#     2.1.1). Pin ≥2.2.0 so pip never tries to build from source on Win.
#   * numpy 1.26.4 is the first 1.26.x with a Python 3.12 stable wheel
#     across all platforms. Keep <2 because pandas 2.2.x (Python ≥3.12)
#     itself constrains numpy<2.
#   * pandas-ta is INTENTIONALLY NOT in this list. The codebase computes
#     RSI/MACD/Bollinger/SMA/EMA with pure pandas (``close.ewm(...)``,
#     ``close.rolling(...)``) in ``analysis/technical.py``. Adding
#     pandas-ta would only re-introduce the numpy 2.x conflict its newer
#     0.4.x betas trigger.
pandas>=2.2.0,<3.0
numpy>=1.26.4,<2.0

# Charts
matplotlib>=3.8.0,<4.0
mplfinance>=0.12.10b0

# Reports
reportlab>=4.0.0
openpyxl>=3.1.0

# Quant / ML
hmmlearn>=0.3.0
arch>=6.0
