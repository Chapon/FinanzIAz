# Runtime dependencies for FinanzIAs.
# Use pinned versions in requirements.lock for reproducible installs.
# Run scripts/audit_dependencies.py periodically to check for CVEs.

# UI
# PyQt6 capeado a 6.7.x: 6.8+ (probado 6.11.0) rompe en Windows con
# "DLL load failed importing QtCore" (ERROR_PROC_NOT_FOUND, mismatch de Qt6).
# 6.7.1 es la version validada en el entorno real. Subir es parte de la tarea
# de upgrade de deps del backlog.
PyQt6>=6.7.1,<6.8

# Market data
yfinance>=0.2.40           # frequent releases — Yahoo Finance API drifts
requests>=2.32.0           # was 2.31.0; bumped for CVE-2024-35195 (cert verify)

# Database
SQLAlchemy>=2.0.0,<3.0
# T7.3: alembic pasó de dev-only a runtime — init_db corre `upgrade head` en
# cada arranque (único camino de esquema desde la migración catch-up 0004).
alembic>=1.13

# Data wrangling
#   * pandas 2.2.2 es la primera release con soporte numpy-2.0 y wheels para
#     3.9-3.13; pin >=2.2.2 para que pip nunca compile desde fuente en Windows.
#   * numpy CAPEADO a <2.0: el entorno real validado (suite verde) corre
#     numpy 1.26.4 + scikit-learn 1.4.2, y sklearn 1.4.2 esta compilado contra
#     numpy 1.x. Subir a numpy 2.x va atado al bump de scikit-learn (que ademas
#     remueve CalibratedClassifierCV(cv="prefit"), usado por ml_signals.py) y es
#     la tarea de upgrade de deps del backlog. El codigo no usa APIs removidas
#     en numpy 2.0 (auditado: np.float_, np.NaN, np.in1d, np.trapz -> cero usos).
#   * pandas-ta is INTENTIONALLY NOT in this list. The codebase computes
#     RSI/MACD/Bollinger/SMA/EMA with pure pandas (``close.ewm(...)``,
#     ``close.rolling(...)``) in ``analysis/technical.py``. Adding
#     pandas-ta would re-introduce a numpy 2.x conflict its betas trigger.
pandas>=2.2.2,<3.0
numpy>=1.26.4,<2.0

# Columnar cache backend (ARQ1) — cache histórico OHLCV en Parquet por (ticker,
# period, interval) + DuckDB como motor analítico sobre el directorio de parquets.
# Reemplaza el JSON-en-SQLite (pd.read_json en cada lectura). Ambas son self-
# contained: pyarrow depende solo de numpy (compatible con el cap <2.0; NO fuerza
# el bump) y duckdb no tiene deps. Ver docs/arq1_parquet_cache.md.
pyarrow>=16.0
duckdb>=1.0

# Charts
matplotlib>=3.8.0,<4.0
mplfinance>=0.12.10b0

# Reports
reportlab>=4.0.0
openpyxl>=3.1.0

# Quant / ML
hmmlearn>=0.3.0
arch>=6.0
# scikit-learn: dependencia transitiva (hmmlearn/arch) que fijamos explicito.
# CAPEADA a <1.8 porque 1.8 removio CalibratedClassifierCV(cv="prefit"), que usa
# analysis/ml_signals.py (_build_calibrator). El entorno real corre 1.4.2.
# Levantar el cap (y migrar a FrozenEstimator) es la tarea de upgrade del backlog.
scikit-learn>=1.4.2,<1.8
# xgboost: clasificador ML del engine (buy_score / ml_probability). El codigo
# degrada con gracia si falta (ml_signals._XGB_OK -> 0.5 neutral), pero el
# entorno real lo corre siempre ON (ver CLAUDE.md). 3.x es la linea validada.
xgboost>=2.0,<4.0

# Optional — LLM catalyst classifier backend (Sprint 5 · T-CAT-2).
# Only needed to run `classify_catalysts.py --backend llm|hybrid`; the default
# heuristic backend needs nothing. Install on demand: `pip install anthropic`
# and set ANTHROPIC_API_KEY. Left commented so it isn't force-installed.
# anthropic>=0.40
